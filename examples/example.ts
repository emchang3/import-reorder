/* tslint:disable-some-rule */

import * as React from 'react';
import { sol } from 'some-vendor-library';
import { OtherDep, ControllerModule } from 'platform-core';
/**
 * Multi-line
 * comment
 * in the middle
 */
import { LibB, LibC, LibA } from 'platform-libs';
import {
  ToolkitSecondaryButton,
  ToolkitPrimaryButton,
  ToolkitButtonSize
} from 'toolkit-button';
import { ToolkitCardContent, ToolkitCard } from 'toolkit-card';
// some
// comments
const outOfPlace = assignment(func);

outOfPlace.doSomethingCool();

import { SecondPageStore } from 'stores/second-page-store';
import { FirstPageStore } from 'stores/first-page-store';
import { RandomViewModel } from 'models';
import { RenderMode } from 'types/constants';
import { SomeButton } from 'components/some-button';
import { Sibling } from './sibling';

// Code below
export const Asdf = () => {
  // Do some calculations

  return (
    <div>
      Stubbed content
    </div>
  );
};
